cmake_minimum_required(VERSION 3.15)
project(simple_sum_op)

set(CMAKE_CXX_STANDARD 17)

# Build simple standalone sum test
add_executable(simple_sum_test simple_test.cpp)

# Build library version
add_library(simple_sum_op INTERFACE)
target_include_directories(simple_sum_op INTERFACE ${CMAKE_CURRENT_SOURCE_DIR})

# Install headers
install(FILES simple_sum.hpp DESTINATION include/aten_ops)